{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "knex migrate:rollback && knex migrate:latest && knex seed:run && nodemon index.js",
    "start:dev": "export NODE_ENV=development && knex migrate:rollback && knex migrate:latest && knex seed:run && nodemon --watch ./ ./src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "nodemon": "^3.1.4",
    "pg": "^8.12.0"
  }
}
